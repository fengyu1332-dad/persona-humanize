---
name: persona-humanize
description: "消除文本「AI味」、注入「真人感」的改写技能。输入一段文字与作者背景信息（年龄、学历、工作、籍贯、语言特点、性格特点等），识别并消除其中的 AI 痕迹、AI 语气、AI 习惯表达与机械感，再结合作者背景推导其专属语言风格画像，输出符合作者身份、无 AI 味、有真人感的中文或英文作品（输出语言与输入保持一致，中英文分别按各自文化语境与地道表达处理）。当用户要求「去AI味」「消除AI感/机器感」「AI改写」「改得像真人写的」「humanize」「de-AI」「降AI浓度」「润色得像人写的」「去掉一股AI腔」等时调用。"
agent_created: true
---

# 消除 AI 味（Remove AI Flavor）

## 一、技能定位

把「一看就是 AI 写的」文字，改写成「一看就是某个具体的人写的」文字。

核心区别在于：AI 追求**正确、完整、平衡、规范、对称**；真人写作是**有观点、有情绪、有取舍、有个人印记、有具体细节，甚至有不完美**。本技能的本质，是把前者改造成后者。

两条不可违背的红线：
1. **忠于原意**：不改事实、不改核心信息、不增删关键结论；只改「怎么说」，不改「说了什么」。
2. **语言一致**：输出语言必须与输入语言保持一致（中文进中文出、英文进英文出），且必须符合该语言的文化语境与地道习惯，绝不中英混杂或用翻译腔。

## 二、触发场景

用户提供文字，并要求「去 AI 味 / 改得像真人 / 消除机器感 / humanize / 降 AI 浓度」等；通常还会附上作者背景信息（年龄、学历、职业、籍贯、语言特点、性格等），用于定制改写风格。

## 三、执行流程（五步，按序执行）

### Step 1 — 解析输入

先明确四件事，缺一不可：
- **语言**：中文 / 英文 / 中英混合。判定后锁定输出语言。
- **作者背景**：从用户描述中提取所有可用维度（年龄、学历、工作、籍贯、语言特点、性格特点等）。缺项先记录，后按默认画像补齐。
- **文本类型**：朋友圈 / 小红书 / 微博 / 公众号文章 / 邮件 / 工作报告 / 学术 / 商业文案 / 评论回复 / 小说 / 演讲稿……不同文体的「真人感」标准不同，改写时要对齐文体。
- **改写强度**：轻度（只去套话）／中度（去套话 + 注入个人语气）／重度（整体重写成作者口吻）。用户未说明时默认**中度**。

### Step 2 — AI 味诊断

加载对应语言的特征清单，逐句扫描：
- 中文 → `references/ai-flavor-patterns-zh.md`
- 英文 → `references/ai-flavor-patterns-en.md`

产出一份简短诊断：标记出「哪些词 / 句式 / 结构暴露了 AI 味」，作为改写依据。诊断报告默认**极简**（几条要点即可），仅在用户明确要求时才展开成完整清单。

### Step 3 — 构建作者语言画像

加载 `references/persona-style-guide.md`，把作者背景信息逐项映射成语言风格参数：
- 年龄 → 年代用词与语气年代感
- 学历 → 词汇复杂度、逻辑严密程度
- 工作 → 行业用语、职业口癖
- 籍贯 → 方言语气词、地域表达习惯
- 语言特点 → 句式长短、口语化程度、成语/网络语偏好
- 性格 → 情绪浓度、直接或委婉、幽默与否

背景信息缺失的维度，用默认画像补齐，并在交付说明中标注「此处为假设」。若背景信息完全缺失，按「30-40 岁、普通话、口语自然、态度明确」的中性真人画像处理。

### Step 4 — 重写（核心动作）

逐段重写。每一句都过以下「去 AI 味」清单，同时贴合 Step 3 的画像：

1. **删套话连接词**：清空「首先/其次/最后、综上所述、值得注意的是、不难发现、由此可见、不仅…而且…」这类逻辑脚手架，让人物关系靠内容自然推进。
2. **打破工整结构**：段落长短参差、句长错落。拒绝每段字数接近、每段「总-分-总」的模板感。
3. **注入个人态度**：加入「我觉得、说实话、其实、说白了」等主观锚点；观点要有偏向、有取舍，不一碗水端平。
4. **具体化**：把抽象名词换成具体的人、事、数字、场景。AI 爱抽象（「赋能」「生态」「闭环」），真人爱具体（「上周三那个客户」「第三行报错」）。
5. **加口语与口癖**：补上语气词、口头禅、filler（中文「嘛、吧、啦、呢、啊、哦」；英文 contractions、well、you know）。允许必要的重复和省略。
6. **允许不完美**：保留倒装、口语残缺句、自我修正的痕迹；这是「人味」的来源，不是 bug。
7. **去强行升华**：删掉鸡汤式结尾、刻意拔高、对仗排比的强行收束；该停就停，不硬凑金句。
8. **动词换抽象**：主动语态、具体动词替换被动语态和名词化表达。
9. **贴合画像**：用词、口癖、情绪浓度严格对齐 Step 3 的作者画像，让读者能「听出是谁在说话」。

### Step 5 — 校验与交付

重写完成后做一次反向自检：把成品当作输入再过一遍 Step 2，确认没有残留 AI 味、且语言符合画像。

交付格式：
1. **主输出 = 改写后的成品全文**（最重要，直接给，不绕弯）。
2. 紧随其后附一段**极简说明**（3-6 条）：改掉了什么、按什么画像改的、哪些维度是假设。
3. 仅在用户明确要求时，才输出 Step 2 的完整诊断报告或「原文 vs 改写」逐段对照表。

## 四、中英文差异化处理（关键）

中英文的「AI 味」来源不同，必须分别对待，禁止互相套用：

- **中文**：AI 味主要来自「书面语过度规范化 + 官腔套话 + 互联网黑话堆砌 + 逻辑连接词滥用 + 排比对仗强迫症」。真人感靠口语词、方言痕迹、情绪词、具体细节找回。参考 `references/ai-flavor-patterns-zh.md`。
- **英文**：AI 味主要来自「标志性大词（delve/leverage/foster…）+ 过渡词滥用（Moreover/Furthermore…）+ 陈词滥调词组 + 被动语态与名词化 + 拒绝使用 contractions」。真人感靠 contractions、口语短语、idioms、filler words、长短句节奏找回。参考 `references/ai-flavor-patterns-en.md`。

改写时先确定语言，只加载该语言的清单；画像映射（Step 3）同样要落到该语言的具体表达上，而非套用另一语言的语气词。

## 五、常见错误提醒

- **不要只做「同义词替换」**：把「赋能」换成「帮助」只是治标，句式结构、段落节奏、观点偏向不变，AI 味依然在。
- **不要过度口语化**：作者是严谨的教授，就不能满屏「嘛」「吧」；口语化程度必须由画像决定，不是越土越像人。
- **不要改变原文信息**：改写是「换说法」，不是「换内容」。
- **中英不要混**：中文成品里别夹英文大词，英文成品里别出现中式英语（Chinglish）结构。

## 六、资源清单

- `references/ai-flavor-patterns-zh.md` — 中文 AI 味特征词库与替换建议
- `references/ai-flavor-patterns-en.md` — 英文 AI 味特征词库与替换建议
- `references/persona-style-guide.md` — 作者背景 → 语言风格画像的映射方法论
